# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# GenLayer / gltest
.gltest_cache/
artifacts/
.pytest_cache/
genlayer-key*.json
*.keystore

# Secrets
.env
.env.*
!*.env.example

# Local-only reference docs — NEVER pushed (stay on this machine)
aegis-wallet-integration.md

# Lab-pair deploy card (ephemeral addresses; redeploy per demo) — never pushed
lab-card.json

# Node (if any frontend tooling appears)
node_modules/
# build.mjs output (Vercel serves this; env-injected config)
frontend/dist/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
